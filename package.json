{
  "name": "@acandrade/ac-bot-avatar",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "^2.0.4",
    "eslint": "^6.8.0",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=10",
    "npm": ">=6"
  }
}